# 安全与隐私

禁止提交真实人脸图片、特征向量、FAISS索引、数据库转储、访问日志、身份证件、API密钥、JWT密钥、`.env`文件以及小组成员个人隐私信息。

访客流程会处理身份信息，人脸识别流程会处理生物特征数据。实际部署应依据适用法律法规与学校制度，落实知情同意、最小化收集、保存期限、访问控制、加密、审计、事件响应和删除机制。

公开发布前运行：

```bash
bash scripts/public_release_audit.sh
```

若密钥曾进入工作目录或Git历史，即使后续删除，也应立即在对应服务端撤销并重新生成。
